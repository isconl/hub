# Copy to .env and fill in before `docker compose up -d`. Never commit the
# real .env -- it's already in .gitignore.
#
# THE ONLY REQUIRED VALUES ARE THE FOUR BITWARDEN ONES BELOW. Every engine
# pulls its own auth token, Jira config, Render key, etc. from Bitwarden
# Secrets Manager at boot (see each repo's lib/secrets.js) -- a fresh clone
# on a new machine needs nothing else to have a working fleet on localhost.

# Bitwarden Secrets Manager -- the one bootstrap credential everything else
# resolves from. Same five values for all seven containers (BE26082009
# added `media` as the 7th, 22 Aug 2026).
BWS_ACCESS_TOKEN=
BWS_ORGANIZATION_ID=
# Region matters: an org on the EU cloud (vault.bitwarden.eu) fails silently
# with "invalid_client" against the US default if these are left blank --
# this was a real, previously-invisible bug in every container. Leave blank
# only if your org is genuinely on the US cloud.
BWS_API_URL=https://api.bitwarden.eu
BWS_IDENTITY_URL=https://identity.bitwarden.eu
# Needed only for WRITE-BACK to Bitwarden (persistSecret): vault's PIN
# reset route (POST /auth/set-pin) and its OAuth refresh-token rotation both
# go through this. Everything read-only (pulling VAULT_TOKEN, TOTP_SECRET,
# etc.) works without it -- but leaving it blank means a PIN reset or a
# Graph token refresh fails with "BWS_PROJECT_ID not set" instead of
# actually writing the new value, which used to fail silently until this
# was traced (2026-08-15). Same project id for all six containers.
BWS_PROJECT_ID=

# ── Everything below is optional ──────────────────────────────────────────
# Each engine falls back to the matching Bitwarden secret (VAULT_TOKEN,
# PULSE_TOKEN, SCOPE_TOKEN, CIRCLE_TOKEN, SPARK_TOKEN, MEDIA_TOKEN, HUB_TOKEN)
# when these are left blank, so a fresh clone works without setting any of
# them. Override here only to use a different token than the one in
# Bitwarden (e.g. a throwaway value for local-only testing).
VAULT_TOKEN=
PULSE_TOKEN=
SCOPE_TOKEN=
CIRCLE_TOKEN=
SPARK_TOKEN=
MEDIA_TOKEN=
HUB_TOKEN=

# media's local library root, bind-mounted into the container (optional --
# defaults to an empty ./library if unset; OneDrive playback needs no
# config here at all, see manifest.js's header in the media repo)
MEDIA_LOCAL_DIR_HOST=

# scope's Jira integration (optional -- scope degrades gracefully without it;
# falls back to Bitwarden's JIRA_HOST/JIRA_EMAIL/JIRA_PROJECT if unset)
JIRA_HOST=
JIRA_EMAIL=
JIRA_PROJECT=

# hub's Render "update power" (optional -- falls back to Bitwarden's
# RENDER_API_KEY; redeploy endpoints degrade gracefully without either)
RENDER_API_KEY=

# vault's backup loop -- how often (ms) it snapshots the encrypted sqlite
# DB and pushes it to OneDrive (BI26083005; was VAULT_SYNC_INTERVAL_MS's
# pull-every-collection loop before OneDrive became backup-only).
# docker-compose.yml already defaults this to 1800000 (30 min) if left
# blank. Override only if 30 minutes is wrong for your recovery-point
# objective -- e.g. a throwaway/offline dev box where you'd rather trigger
# it by hand via POST /backup/run. Only meaningful on VAULT_STORE_ENGINE=sqlite.
VAULT_BACKUP_INTERVAL_MS=
