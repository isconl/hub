# STAGED - not yet active.
#
# When ARCHITECT cues the main push, copy this file to .github/workflows/ on the
# MAIN branch. From then on, every push to main also rebuilds the APK on the
# apk branch (workflow_dispatch is exempt from GitHub's GITHUB_TOKEN
# no-retrigger rule, so the default token is enough - no PAT needed).

name: Trigger APK build

on:
  push:
    branches: [main]

permissions:
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Cue the apk branch build
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run "Build APK" --repo "$GITHUB_REPOSITORY" --ref apk
